// Placeholder — speed benchmarking via criterion
// Will be populated once scoring function stabilizes
fn main() {
    eprintln!("Use `cargo run --release --bin bm199-eval` for speed benchmarks");
}
